/*
 * This is a manifest file that'll be compiled into application.css, which will include all the files
 * listed below.
 *
 * Any CSS and SCSS file within this directory, lib/assets/stylesheets, vendor/assets/stylesheets,
 * or any plugin's vendor/assets/stylesheets directory can be referenced here using a relative path.
 *
 * You're free to add application-wide styles to this file and they'll appear at the bottom of the
 * compiled file so the styles you add here take precedence over styles defined in any styles
 * defined in the other CSS/SCSS files in this directory. It is generally better to create a new
 * file per style scope.
 *
 */
 // import bootstrap-sprokets before bootstrap if using bootstrap >= 3.2
@import "bootstrap-sprockets";
@import "bootstrap";
@import 'bootstrap/theme';
@import "nav.scss";
@import 'bootstrap-datetimepicker';
//= depend_on_asset "bg1.jpg"

.alert-alert {
  @extend .alert-warning;
}

.alert-notice {
  @extend .alert-info;
}


// url(bg1.jpg)
body {
    background-image:url('<%= asset_path("bg1.jpg") %>');
}


// google-map window details, required to render map in div
#map {
    width: 640px;
    height: 480px;
    border: .5px solid navy;
    margin: 0 auto; // center
  }


  /* line 7, bootstrap-sass-3.3.6/assets/stylesheets/bootstrap/_panels.scss */
  .panel {
    margin-bottom: 20px;
    color: #3f3f3f;
    background-color: #f8f8f8; // fbfb -> f3f3 overiding pure white in form panels
    opacity: 1.0; // full opacity @ 1.0 ---> less opacity @ 0.1
    border: 1px solid transparent;
    border-radius: 4px;
    -webkit-box-shadow: 0 1px 1px rgba(0, 0, 0, 0.05);
    box-shadow: 0 1px 1px rgba(0, 0, 0, 0.05);
  }

// DateTime_Picker class in _form.html.erb used for Date input-group
  .input-group {
    width: 13em;
  }

// Not black header text
.thead-index-header {
  // padding-bottom: 6px;
  font-size: .9em;
  // color: #2f082b;
    color: #cea6ca;
    // background-color: #eeeeee;
    // opacity: 0.5;
}
